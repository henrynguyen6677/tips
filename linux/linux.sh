# set process sort by memory usage
watch -n 2 ps aux --sort -rss

# list all users
cut -d: -f1 /etc/passwd

# copy and keep permission
# good in case for deep permission paths
cp -pr

# Ubuntu

# Network
# Check port list
sudo netstat -plnt
